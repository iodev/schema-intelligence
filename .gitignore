# Dependencies
node_modules/
yarn.lock
pnpm-lock.yaml

# Build output
# Note: dist/ is committed so consumers via 'github:iodev/schema-intelligence'
# can import without running a build. yarn v1 doesn't install devDependencies
# for git-URL deps, so the 'prepare' script can't compile TypeScript on its own.
dist-test/
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pino-debug.log*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
coverage/
.nyc_output/

# Misc
*.tmp
*.temp
